# Route Transitions

This package help the use of transitions between routes.

## Example

```
/// [context] BuilContext
RouteTransitions(
  context: context, 
  child: Page2(),
  animation: AnimationType.fadeIn,
);
```
